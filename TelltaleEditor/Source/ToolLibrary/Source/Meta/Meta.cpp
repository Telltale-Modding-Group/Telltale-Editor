#include <Meta/Meta.hpp>

namespace Meta {
    
    // ScriptManager in development. Once that is up to scratch we can implement the game independent meta system
    
    void Initialise(ToolContext& ctx)
    {
        
    }
    
    void Shutdown(ToolContext& ctx)
    {
        
    }
    
}
